package com.foxnet.medications.database

/** Stable medication categories; display names belong in the UI rather than the database. */
enum class MedicationType {
    CAPSULE,
    INJECTION,
    TABLET,
    LIQUID,
    OTHER,
}
